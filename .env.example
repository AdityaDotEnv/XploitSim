# XploitSim - Example Environment Variables

# --- BACKEND SETTINGS ---
# The secret used for JWT signing and other cryptographic operations.
JWT_SECRET=your_super_secure_secret_here

# Port for the API Gateway (default: 5000)
PORT=5000

# Other ports for individual microservices (internal use)
# AUTH_PORT=4100
# GATEWAY_PORT=5000
# ... (see .env for full list)

# --- FRONTEND SETTINGS ---
# The URL of your deployed backend server. 
# In local development, this defaults to http://localhost:5000
VITE_API_URL=https://your-backend-server.com
